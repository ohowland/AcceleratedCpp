// class design often begins with specifing the interface.
// Determine the interface by looking at the situations 
// where we would like to call the class.

// construct, const_iteration, size_type, use index operator,
// begin() & end()
